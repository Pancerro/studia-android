<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aktywnosciiintencje">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main3Activity" android:label="@string/wynik"></activity>
        <activity android:name=".Main2Activity" android:label="@string/wybierz_liczby"/>
        <activity android:name=".MainActivity" android:label="@string/send">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <data android:mimeType="text/plain"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>